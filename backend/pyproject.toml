[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]

[tool.ruff.lint.flake8-bugbear]
# FastAPI 的 Depends 默认参数是框架惯用法，不是 B008 要拦的可变默认值
extend-immutable-calls = ["fastapi.Depends"]

[tool.black]
line-length = 100
target-version = ["py313"]
